# Backend API Base URL (Port 5001 per team specification)
NEXT_PUBLIC_API_URL=http://localhost:5001/api

# Set to false to connect directly to live backend (with mock AI / MATLAB bridge)
# Set to true for self-contained offline browser demo
NEXT_PUBLIC_USE_MOCK=false
